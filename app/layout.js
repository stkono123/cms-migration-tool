export const metadata = {
  title: 'CMS Migration Tool',
  description: 'AI-powered Shopify to Contentful migration',
}

export default function RootLayout({ children }) {
  return (
    <html lang="de">
      <body>{children}</body>
    </html>
  )
}
